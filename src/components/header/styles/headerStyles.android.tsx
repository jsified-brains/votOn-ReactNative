import { headerStyles, HeaderStyle } from './headerStyles.common';
import { StyleSheet } from 'react-native';
// import { AppDeviceInfo } from '../../../modules';

export const android_styles = StyleSheet.create({});

export const androidStyles: HeaderStyle =  {...headerStyles, ...android_styles };
