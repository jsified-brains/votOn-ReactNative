import { StyleSheet } from 'react-native';
// import { styleType } from './styles';
// export const AndroidStyles: styleType = StyleSheet.create({
export const AndroidStyles = StyleSheet.create({
    card: {
        padding: 10
    }
});
